[project]
name = "project1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aman Kumar", email = "amankumarsah.id@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "groq>=1.6.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "pypdf>=6.0.0",
    "python-docx>=1.2.0",
    "streamlit>=1.61.1",
]

[project.scripts]
project1 = "project1:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
